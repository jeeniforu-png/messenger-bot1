## Packages
framer-motion | Essential for smooth, premium animations in a minimal interface
react-textarea-autosize | Creates elegant, auto-expanding text areas that feel like Apple Notes
date-fns | For formatting the note creation dates cleanly
lucide-react | Already installed, but noting we will use it heavily for minimal iconography

## Notes
Tailwind Config - Please ensure these font families are available if needed, though we apply them via standard CSS variables.
The frontend uses a pure minimal monochrome aesthetic with heavy reliance on whitespace, layout, and typography over visible borders.
